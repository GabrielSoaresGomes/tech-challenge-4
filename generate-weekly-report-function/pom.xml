<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>com.gabriel.soares</groupId>
    <artifactId>generate-weekly-report-function</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <java.version>17</java.version>

        <azure.functions.maven.plugin.version>1.37.1</azure.functions.maven.plugin.version>
        <azure.functions.java.library.version>3.1.0</azure.functions.java.library.version>

        <functionAppName>generate-weekly-report-function</functionAppName>
        <stagingDirectory>${project.build.directory}/azure-functions/${functionAppName}</stagingDirectory>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.microsoft.azure.functions</groupId>
            <artifactId>azure-functions-java-library</artifactId>
            <version>${azure.functions.java.library.version}</version>
        </dependency>

        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <version>42.7.8</version>
        </dependency>

        <dependency>
            <groupId>com.github.librepdf</groupId>
            <artifactId>openpdf</artifactId>
            <version>1.3.32</version>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>5.10.0</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>com.azure</groupId>
            <artifactId>azure-communication-email</artifactId>
            <version>1.1.0</version>
        </dependency>

        <dependency>
            <groupId>com.sun.mail</groupId>
            <artifactId>jakarta.mail</artifactId>
            <version>2.0.1</version>
        </dependency>

        <dependency>
            <groupId>com.microsoft.azure</groupId>
            <artifactId>applicationinsights-core</artifactId>
            <version>2.6.4</version>
        </dependency>

        <dependency>
            <groupId>com.azure</groupId>
            <artifactId>azure-storage-blob</artifactId>
            <version>12.25.3</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>

            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                </configuration>
            </plugin>

            <plugin>
                <groupId>com.microsoft.azure</groupId>
                <artifactId>azure-functions-maven-plugin</artifactId>
                <version>${azure.functions.maven.plugin.version}</version>
                <configuration>
                    <resourceGroup>${azure.functions.resourceGroup}</resourceGroup>
                    <appName>${azure.functions.appName}</appName>
                    <region>${azure.functions.region}</region>

                    <runtime>
                        <os>${azure.functions.os}</os>
                        <javaVersion>${java.version}</javaVersion>
                    </runtime>
                </configuration>

                <executions>
                    <execution>
                        <id>package-functions</id>
                        <phase>package</phase>
                        <goals>
                            <goal>package</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

        </plugins>
    </build>
    <profiles>
        <profile>
            <id>local</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>
                <azure.functions.resourceGroup>rg-local-dummy</azure.functions.resourceGroup>
                <azure.functions.appName>func-local-dummy</azure.functions.appName>
                <azure.functions.region>brazilsouth</azure.functions.region>
                <azure.functions.os>windows</azure.functions.os>
            </properties>
        </profile>

        <profile>
            <id>dev</id>
            <properties>
                <azure.functions.resourceGroup>rg-tech-challenge</azure.functions.resourceGroup>
                <azure.functions.appName>func-tech-challenge-weekly-report</azure.functions.appName>
                <azure.functions.region>brazilsouth</azure.functions.region>
                <azure.functions.os>linux</azure.functions.os>
            </properties>
        </profile>

        <profile> <!-- Mudar quando criarmos a function no ambiente definitio -->
            <id>prod</id>
            <properties>
                <azure.functions.resourceGroup>rg-tech-challenge-prod</azure.functions.resourceGroup>
                <azure.functions.appName>func-tech-challenge-weekly-report-prod</azure.functions.appName>
                <azure.functions.region>brazilsouth</azure.functions.region>
                <azure.functions.os>linux</azure.functions.os>
            </properties>
        </profile>
    </profiles>
</project>
